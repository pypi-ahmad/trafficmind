# Apps

This directory contains application entrypoints and product-facing services.

- `web/` — canonical web product location
- `api/` — FastAPI HTTP and WebSocket service
- `workflow/` — LangGraph workflow service

The existing runnable Next.js app currently remains in `../frontend` until it is migrated deliberately.
