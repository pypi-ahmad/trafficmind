# Web App

This directory is the standardized home for the TrafficMind web application.

The current runnable Next.js implementation still lives in `../../frontend` to preserve existing startup paths during the repository restructure. Future web migration work should target this folder.
