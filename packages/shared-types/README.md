# Shared Types

Use this package for repository-wide schemas and contracts.

Recommended contents:

- OpenAPI-generated client types
- JSON Schema documents
- shared event, violation, and zone contracts

Avoid hand-maintaining duplicated runtime models in multiple languages when generation from a single source is possible.
